
version: "1"
name: build_bundle_artifact
steps:
  - id: prepare
    run: prompt
    with:
      prompt_file: prompts/bundling/plan.md
      out: out/plan.md
  - id: pack
    run: shell
    with:
      cmd: |
        zip -r artifact.zip ../..
